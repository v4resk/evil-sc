Sub CesarDecode####UUID####(buf)
    Dim i, shift

    shift = ####SHIFT####

    ' Loop through each byte in the buffer
    For i = LBound(buf) To UBound(buf)
        ' Reverse Caesar shift with wraparound
        buf(i) = (buf(i) - shift + 256) Mod 256
    Next
End Sub
