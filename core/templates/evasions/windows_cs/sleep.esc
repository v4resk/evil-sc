public static class GetTimeInfo####UUID####
{
        public static void DoTimeSleep()
    {
            uint sleepTime = ####SLEEP_TIME####;
            double verifyTime = ####VERIFY_TIME####;
            DateTime longbefore = DateTime.Now;
            Thread.Sleep((int)sleepTime);
            if (DateTime.Now.Subtract(longbefore).TotalSeconds < verifyTime)
            {
                System.Environment.Exit(0);
            }
    }
}